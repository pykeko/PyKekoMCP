{
  "name": "pykeko-mcp",
  "version": "0.3.0",
  "description": "MCP server to drive a running PyKeko (Moorhen-based Coot) desktop app",
  "type": "module",
  "bin": { "pykeko-mcp": "dist/server.js" },
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "@types/node": "^20.14.0"
  }
}
